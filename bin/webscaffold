#!/usr/bin/env node

const { createProject } = require('../lib/create');
const [, , ...args] = process.argv;

if (args[0] === 'create') {
    createProject(args[1]);
} else {
    console.error('Invalid command. Usage: webscaffold create <project-name>');
}
